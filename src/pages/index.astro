---
import { posts } from "../../utils/posts"
import RootLayout from "../layouts/RootLayout.astro"
import useFormattedDate from "../hooks/useFormattedDate"

const currentDate = new Date()

const today = useFormattedDate(currentDate)
---

<RootLayout title='Astro 🚀'>
  <div class='p-24 mx-auto max-w-screen-lg'>
    <div class='px-5 flex justify-between items-center'>
      <h1 class='text-base'>Astro 🚀</h1>
      <a
        href='/about'
        class='text-sm text-gray-500 hover:text-orange-600 hover:underline hover:decoration-orange-400 transition-colors'
        >About</a
      >
    </div>
    <p class='px-5 text-xs text-gray-400'>{today}</p>

    <!-- <div class='my-12 w-full border-t border-gray-300'></div> -->

    <ul class='mt-12 space-y-10'>
      {
        posts.map((post) => (
          <li class='py-4 px-5 group hover:bg-orange-100/50 rounded-xl'>
            <a href={`/posts/${post.link}`} class='w-fit h-fit space-y-1'>
              <span class='text-xs text-gray-400 uppercase'>
                {post.category}
              </span>
              <h2 class='text-xl font-semibold text-gray-800 group-hover:text-orange-500'>
                {post.title}
              </h2>
              <p class='text-sm text-gray-500 line-clamp-3 group-hover:text-gray-800'>
                {post.summary}
              </p>
              <div class='flex items-center gap-2 text-xs text-gray-400 '>
                <span>{useFormattedDate(post.publishedOn)}</span>
                <p>·</p>
                <span>{post.readingDuration} min</span>
              </div>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</RootLayout>
